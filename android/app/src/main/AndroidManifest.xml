<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.lovable.klasspuls">

    <!-- POST_NOTIFICATIONS permission for Android 13+ (SDK >= 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- VIBRATE permission for notification vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- WAKE_LOCK permission for scheduled notifications -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Internet permission (if needed for app functionality) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">
        
        <!-- Main Activity -->
        <activity
            android:name="app.lovable.klasspuls.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
    </application>
</manifest>
